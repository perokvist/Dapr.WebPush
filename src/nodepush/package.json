{
  "name": "nodepush",
  "version": "0.0.0",
  "description": "nodepush",
  "main": "server.js",
  "author": {
    "name": ""
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "dev": "ts-node-dev --inspect --ignore-watch node_modules server.ts",
    "start": "dapr run --app-id=nodepush --app-port=3000 -- ts-node-dev --inspect --ignore-watch node_modules server.ts"
  },
  "devDependencies": {
    "@types/node": "^8.0.14",
    "ts-node-dev": "^1.0.0",
    "typescript": "^4.0.5"
  },
  "dependencies": {
    "dapr-client": "^0.7.0-preview01",
    "express": "^4.17.1",
    "@types/google-protobuf": "^3.7.2",
    "@types/node": "^14.0.5"
  }
}
